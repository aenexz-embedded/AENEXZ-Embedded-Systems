RCC->APB2ENR   =  Enable GPIO Clock

GPIOC->CRH   = Configure GPIO Input

GPIOC->ODR  =  Output Data Register

Set bit at position n:

  REG |= (1 << n);

Clear bit at position n:

  REG &= ~(1 << n);

Toggle bit at position n:

  REG ^= (1 << n);

Read bit at position n:

  (REG >> n) & 1